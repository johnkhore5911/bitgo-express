{
  "name": "bitgo-express",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node node_modules/bitgo/bin/bitgo-express --env prod --port $PORT --bind 0.0.0.0 --access-token $BITGO_TOKEN",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bitgo": "47.0.0"
  }
}
